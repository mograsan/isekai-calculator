import 'package:flutter/material.dart';

class Button extends StatelessWidget {
  String _character;
  Color _fontColor;
  Color _backgroundColor;

  Button(this._character, this._fontColor, this._backgroundColor, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
    );
  }
}